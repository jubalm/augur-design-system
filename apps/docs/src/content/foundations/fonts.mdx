---
# Fonts and typography (issue #7 content, migrated to the shared content
# pipeline in #8 — src/content/foundations/, route /foundations/fonts).
# The live samples render through real package consumers; code samples
# are derived from the same example modules that render (see
# src/content/README.md, "Live examples").
title: Fonts and typography
description: The two voice families and seven typography roles of the Augur system, self-hosted and rendered live from @augur/design-system.
order: 1
---
import { DocExample } from "../../components/DocExample";
import { examples } from "../../examples/registry";
import { FontRoles } from "../../components/FontRoles";

Augur uses two contemporary sans-serif families with distinct roles.
**Sora** is the primary voice: display, headings, controls, key actions,
and primary navigation. **Schibsted Grotesk** is the supporting voice:
paragraphs, helper text, metadata, tables, and dense UI text. Families
are never alternated for decoration; each text element has one clear
role. Both are delivered self-hosted through the package stylesheet
entry — no font CDN, no third-party request.

## The seven roles, rendered live

Every sample below is a package typography role class applied to real
markup by the docs app; the values come from the package stylesheet,
not from this page.

<FontRoles />

## Using the voices in code

The example below is rendered live from a real module that imports the
package entry; the code sample is that same module's source, derived
from the same file — they cannot drift.

<DocExample example={examples.fonts.stacks} />

## Order, numerals, and technical strings

Size creates order and weight confirms it. Use sentence case for
headings, controls, and labels, and reserve capitals for short metadata
and standard abbreviations. Do not stack size, weight, color, and
capitals to manufacture hierarchy — one strong distinction and one
secondary distinction are enough. (Where color is one of those
distinctions, the [color system](/foundations/color) rules apply.)

In tables and records: align numeric columns right, use one precision
per view, and put units in a column header or beside the value. For
long technical strings such as addresses, truncate in the middle while
preserving a recognizable prefix and suffix.

## Loading behavior

`import "@augur/design-system/styles.css"` pulls the pinned
`@fontsource` packages (Sora 400/600, Schibsted Grotesk 400) into the
consumer's bundle, so font files are served from the same origin as the
site. The `--augur-font-primary` / `--augur-font-secondary` custom
properties carry fallback stacks, so text remains readable while faces
load. The executable evidence for delivery — `document.fonts.check()`,
computed role values, and the absence of console or network failures —
lives in the package font fixture.

## Provenance

Exact versions, licenses (OFL-1.1), and copyright notices for both
families are recorded in the package's provenance export and in
[resources/brand/PROVENANCE.md](https://github.com/jubalm/augur-design-system/blob/main/resources/brand/PROVENANCE.md).
The machine-readable export is demonstrated on
[Package entries](/reference/package-entries). Redistribution
obligations are tracked with the asset decisions in issue #20.

The foundation proposals that sit alongside typography — spacing,
sizing, radius, focus, and motion — are in
[Foundation proposals](/foundations/proposals), pending maintainer
review; the seven roles on this page, by contrast, are adopted values
recorded in `DESIGN.md`.
