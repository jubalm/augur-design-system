---
/**
 * Stable route for foundation pages (issue #8).
 *
 * /foundations/<slug> ← src/content/foundations/<slug>.md(x)
 * Metadata and body conventions: src/content/README.md.
 */
import { getCollection } from "astro:content";
import type { CollectionEntry } from "astro:content";
import DocPage from "../../components/DocPage.astro";

export async function getStaticPaths() {
  const entries = await getCollection("foundations", ({ data }) => !data.draft);
  return entries.map((entry) => ({ params: { slug: entry.id }, props: { entry } }));
}

interface Props {
  entry: CollectionEntry<"foundations">;
}

const { entry } = Astro.props;
---
<DocPage entry={entry} />
